{
  "name": "mongodb-sandbox",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "vscode",
  "customizations": {
    "vscode": {
      "extensions": [
        "mongodb.mongodb-vscode"
      ]
    }
  },
  "postCreateCommand": "mongod --version && mongosh --version"
}
